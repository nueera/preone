# Database (PostgreSQL via Docker Compose or direct connection)
DATABASE_URL="postgresql://preone:preone_dev_2024@localhost:5432/preone_dev?schema=public"

# Auth (MUST change in production!)
JWT_SECRET="preone-dev-secret-key-2024-change-in-production"
JWT_EXPIRY="24h"

# App
NEXT_PUBLIC_APP_URL="http://localhost:3000"
NEXT_PUBLIC_APP_NAME="PreOne"

# Redis
REDIS_URL="redis://localhost:6379"

# Socket.io CORS
SOCKET_CORS_ORIGINS="*"
